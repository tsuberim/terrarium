name: CI

# PRs only — push to main is covered by deploy.yml (test + optional prod deploy).
on:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    uses: ./.github/workflows/reusable-test.yml
